import { defineAppConfig } from '#imports';

export default defineAppConfig({
  name: 'Malta',
  ui: {
    colors: {
      primary: 'malta',
      secondary: 'hungry',
      neutral: 'stone'
    }
  }
});
