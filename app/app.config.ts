import { defineAppConfig } from '#imports';

export default defineAppConfig({
  name: 'Malta Calendar',
  ui: {
    colors: {
      primary: 'malta',
      secondary: 'hungry',
      neutral: 'stone'
    }
  }
});
